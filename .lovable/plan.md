In `src/components/layout/OwnerSidebar.tsx` (righe 101-112), aggiornare le classi dei `<p>` con il nome utente da `text-sm font-medium` a `font-display uppercase font-normal text-[15px]` (mantenendo `text-white truncate`). Applicato a tutte e tre le varianti (firstName, lastName, fallback email/Admin).

Nessun altro cambiamento.