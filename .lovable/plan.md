## Modifica

In `src/components/castings/rounds/RoleRoundsCompartment.tsx`, cambiare la classe del titolo del ruolo (attualmente `text-base`, 16px) in `text-2xl` (24px, classe standard Tailwind del DS), mantenendo `font-display uppercase tracking-wider`.

Nessun'altra modifica.